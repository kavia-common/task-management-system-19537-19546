# PUBLIC INTERFACE: Environment configuration for the React frontend
# Configure the base URL of the backend API (no trailing slash).
# Example: http://localhost:8000
REACT_APP_API_BASE_URL=http://localhost:8000
